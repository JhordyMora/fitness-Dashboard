import React from 'react';
import "../styles/fitness.scss";

const Fitness = ({children}) => {
    return (
        <div class="fitness">
            {children}
        </div>
    )
}

export default Fitness